#include<stdio.h>

int main()
{
    int km;
    char cat;
    float l1, l2, l3;

    printf("insira a kilometragem: ");
    scanf("%d", &km);
    printf("insira a categoria: ")

    l1 = km / 15;
    l2 = km / 12;
    l4 = km / 10;

    if (cat == 'a'){
        printf("a quantidade de litros gasta foi de %f", l1);
    }


    else if (cat == 'b'){
        printf("a quantidade de litros gasta foi de %f", l2);
    }


    else if (cat == 'c'){
        printf("a quantidade de litros gasta foi de %f", l3);
    }
    return 0;
}
