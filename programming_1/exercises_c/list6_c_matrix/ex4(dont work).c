//simple dont work


/*
#include<stdio.h>

void f_fill(int mat[][max], int max)
{
    int i, j;
    for(i = 0; i < max; i++){
        for(j = 0; j < max; j++){
            printf("enter the value of the position [%d][%d]: ", i, j);
            scanf("%d", &mat[i][j]);
        }
    }
}

void f_mult(int mat[][max], int max, int k)
{
    int i, j;
    for(i = 0; i < max; i++){
        for(j = 0; j < max; j++){
            if(i + j == max - 1){
                mat[i][j] *= k;
            }
            else{}
        }
    }
}

void f_print(int mat[][max], int max)
{
    int i, j;
    for(i = 0; i < max; i++){
        for(j = 0; j < max; j++){
            printf("%d - ", mat[i][j]);
        }
    }
}


int main()
{
    int max, k;
    printf("enter the size of the matrix(between 2 and 100): ");
    scanf("%d", &max);
    printf("enter a value to multiply the second diagonal: ");
    scanf("%d", &k);


    int matriz[max][max];

    f_fill(mat max);
    f_mult(mat max, k);
    fgcc_print(mat max);

    return  0;

}

*/